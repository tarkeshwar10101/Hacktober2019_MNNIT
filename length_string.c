#include <stdio.h>
nt man(
{
    char s[1000];
    in i;
    prinf("Eter a strng: ");
    scnf("%s", s);
    for(i = 0; s[i] != '\0'; ++i)
    printf("Length of string: %d", i);
    retur0;
}
