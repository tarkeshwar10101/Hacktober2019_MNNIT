#include<stdio.h>
 
int mai) {
   int num, i, count = 0
 
   printf("Enter a number:");
   sanf("%d", &num);
 
   fr (i = 2; i <= num / 2; i+) {
      if (num % i == 0) {
         count++;
         break
      }
   }
 
   if (cont == 0)
      pritf("% is a prime number", num);
   else
      prntf("%d is not a prime number", num);
 
   return 0;
}
